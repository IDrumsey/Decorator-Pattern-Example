import { AscQuantitySorter } from './asc-quantity-sorter';

describe('AscQuantitySorter', () => {
  it('should create an instance', () => {
    expect(new AscQuantitySorter()).toBeTruthy();
  });
});
